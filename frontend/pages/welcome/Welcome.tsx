import { Heading, Wrapper } from "@/components/common";

import React from "react";

const Welcome = () => {
  return (
    <Wrapper>
      <Heading type={{ title: "Welcome to the application." }} />
    </Wrapper>
  );
};

export default Welcome;
